extends base
include mixins/video

block content
    div.video__grid
        each potato in videos
            +video(potato)
            +video(potato)
            +video(potato)
            +video(potato)
            +video(potato)
            +video(potato)
        else
            span.empty__message No videos found
