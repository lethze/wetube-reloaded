extends base
include mixins/video.pug

block content
    form(method="GET").search__form
        input(placeholder="Search by title", type="text", name="q")
        button
            i.fas.fa-search
    div.video__grid
        each video in videos
            +video(video)
        else
            span.empty__message No videos found.
