extends base

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="Old Pssword" type="password" required name="oldPassword")
        input(placeholder="New Pssword" type="password" required name="newPassword")
        input(placeholder="New Pssword Confirmation" type="password" required name="newPassword2")
        input(value="Change Password" type="submit")
